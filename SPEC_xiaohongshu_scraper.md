你现在是一名资深 Python 自动化工程师 + 浏览器抓取工程师 + 后端工具开发者。请你直接为我生成一个“可运行、可维护、工程化”的 Python 项目，用于自动获取小红书（Xiaohongshu）搜索结果数据，并实现从首次登录到后续自动采集、详情页补全、解析、导出的完整流程。

注意：我要的不是原理说明，不是伪代码，不是示意片段，而是尽可能完整、可运行的项目代码和清晰的项目结构。请你像真实交付一个开源仓库一样完成。

一、项目目标

请你实现一个 Python 项目，功能要求如下：

1. 使用 Playwright 打开 Chromium 浏览器。
2. 支持“首次人工登录”，登录成功后自动保存本地会话状态。
3. 后续再次运行时自动复用本地 session，避免重复扫码登录。
4. 支持输入关键词，自动打开小红书搜索页面。
5. 不通过手写接口签名，不伪造风控参数；而是通过浏览器真实访问页面，并监听网页自身发出的网络请求或响应来获取搜索结果数据。
6. 重点实现对搜索结果页数据的获取，并支持进入笔记详情页补全正文与互动指标。
7. 将提取出的数据清洗后导出为 CSV 和 Excel。
8. 提供 CLI 命令行入口。
9. 可选：再额外提供一个简单的 FastAPI Web UI 版本，但必须保证 CLI 先完整可用。
10. 项目代码要清晰分层，便于后续扩展。
11. 项目优先抓取“公开可见”的笔记内容，不要依赖手工复制 HTML，也不要要求用户自己逐条打开页面粘贴数据。
12. 需要兼顾两类数据来源：
   - 搜索结果页返回的数据
   - 详情页返回的数据
   优先保证“搜索结果页拿到基础列表 + 详情页补全正文和互动信息”这条主链路跑通。

二、必须实现的核心流程

请你完整实现下面这条流程：

启动程序
→ 检查本地是否已有 session/storage_state.json
→ 如果没有或失效，则打开可见浏览器让用户手动登录
→ 登录成功后保存 storage_state
→ 后续运行时加载 storage_state 进入已登录上下文
→ 输入搜索关键词并访问小红书搜索页
→ 监听页面的网络请求或响应
→ 从响应中筛选出搜索结果对应的 JSON 数据
→ 提取笔记基础信息列表
→ 对每条笔记进入详情页或触发详情接口
→ 补全正文、互动数据、发布时间、图片等信息
→ 对结果进行去重、格式化
→ 导出 CSV / Excel
→ 在终端打印统计信息

三、技术要求

请严格按以下技术栈实现：

- Python 3.10+
- Playwright
- pandas
- openpyxl
- typer 或 argparse（二选一，优先 typer）
- 可选 Web：FastAPI + 简单 HTML 页面
- 项目结构必须清晰，禁止把所有逻辑塞进一个文件

建议目录结构如下，但你可以优化：

project_root/
├── main.py                    # CLI 入口
├── server.py                  # 可选，Web 入口
├── requirements.txt
├── README.md
├── scraper/
│   ├── auth.py                # 登录与会话管理
│   ├── search.py              # 搜索页访问与搜索结果捕获
│   ├── detail.py              # 详情页访问与正文/互动数据补全
│   ├── parser.py              # JSON 解析、字段提取、去重
│   ├── exporter.py            # CSV / Excel 导出
│   └── utils.py               # 通用工具函数
├── session/
│   └── storage_state.json     # 本地登录态
├── data/
│   └── *.csv / *.xlsx         # 导出结果
└── static/
    └── index.html             # 可选 Web UI

四、数据获取方式约束

你必须采用以下方式获取数据：

1. 通过 Playwright 驱动浏览器访问真实网页。
2. 通过 page.on("response")、page.on("requestfinished") 或等价机制监听网络请求/响应。
3. 先从搜索结果页获取基础笔记列表。
4. 再从详情页或详情接口返回的 JSON 中提取完整正文、互动指标、发布时间、图片等信息。
5. 如果某些字段在搜索结果页没有，就必须通过详情页补全。
6. 所有抓取都基于浏览器真实访问页面，不依赖伪造签名与逆向参数。

禁止做以下事情：

- 不要伪造登录态
- 不要尝试破解验证码
- 不要实现任何绕过平台安全机制的逻辑
- 不要手写逆向签名算法
- 不要给出规避风控的攻击性建议
- 不要依赖“用户自己手动复制 HTML 给程序”这种低级方案
- 不要假设接口字段永远固定，解析逻辑必须有防御性

五、字段要求

请尽量提取并导出以下字段，若某字段不存在可留空：

基础字段：
- keyword                  # 搜索关键词
- note_id
- note_url
- title
- content                  # 正文，优先详情页补全
- note_type                # 图文 / 视频 / 其他，如可获得
- author_name
- author_id                # 若可获得
- author_url               # 若可拼接
- publish_time
- location                 # 若可获得
- ip_location              # 若可获得
- tags                     # 话题标签，若可获得
- images                   # 图片链接列表，JSON 字符串或分隔文本
- cover_image              # 封面图，若可获得

互动字段：
- like_count
- collect_count
- comment_count
- share_count              # 若可获得
- view_count               # 若可获得

补充字段：
- search_rank              # 在搜索结果中的顺序
- source_page              # 来源页码
- raw_search_json          # 可选，调试用
- raw_detail_json          # 可选，调试用

如果你认为某些字段在真实场景中不一定稳定存在，请用防御式解析处理，并在代码注释中说明。

六、程序行为要求

1. 首次登录命令：
   python main.py login

2. 搜索命令示例：
   python main.py search "哈尔滨 旅游" --pages 3 --format csv
   python main.py search "哈尔滨 美食" --pages 5 --format excel
   python main.py search "中央大街" --pages 5 --detail true

3. 支持参数：
- 关键词 query
- 页数 pages
- 导出格式 format（csv / excel）
- 输出路径 output
- 是否进入详情页补全 detail
- 最小/最大请求间隔
- 是否显示浏览器 headed/headless
- 是否启用调试日志
- 是否保存 raw json
- 每页最大抓取条数（如可控）

4. 每抓取一页要打印进度。
5. 每补全一条详情要打印简洁进度。
6. 要有基本的异常处理。
7. session 失效时，要提示用户重新登录。
8. 页面监听不到目标响应时，要输出明确错误信息，而不是静默失败。
9. 对重复笔记要去重，优先按 note_id 去重。
10. 要对时间、计数、文本字段做尽量稳健的清洗，兼容空值、缺失值、不同结构。
11. 导出前要转成 pandas DataFrame。
12. 如果正文不在搜索结果接口中，必须尝试通过详情页补全，而不是直接留空。
13. 如果某条详情补全失败，不要让整个程序崩溃，应记录失败并继续处理下一条。

七、工程质量要求

请你生成的代码必须满足：

1. 每个文件职责明确。
2. 关键函数带注释与 docstring。
3. 命名清晰，不要使用 a、b、tmp 这类随意变量名。
4. 有基本日志输出。
5. 有 README，写清楚：
   - 安装方式
   - 依赖
   - Playwright 安装命令
   - 首次登录方法
   - 搜索方法
   - 详情页补全方法
   - session 机制说明
   - 常见报错排查
6. requirements.txt 必须完整。
7. 若实现 Web UI，则 API 逻辑要复用抓取核心，不要重复造两套逻辑。
8. 代码优先保证主链路能跑通，不要为了追求花哨结构牺牲可运行性。

八、输出要求

请严格按下面顺序输出：

1. 先给出完整项目目录树
2. 然后逐个文件输出完整代码
3. 每个文件都要用清晰标题标注，例如：
   === main.py ===
   <代码>
4. 代码输出完后，再给 README.md 内容
5. 最后补充运行步骤：
   - pip install -r requirements.txt
   - playwright install chromium
   - python main.py login
   - python main.py search "关键词" --pages 3 --detail true
6. 如果你认为某个接口字段结构在真实环境中可能变化，请在代码里写出“防御式解析”
7. 如果某部分无法 100% 保证字段路径固定，请明确写成可维护的解析函数，不要硬编码得过于脆弱
8. 如果搜索页和详情页使用的是不同结构的数据返回，请分别封装解析函数，不要混在一起写

九、代码生成策略要求

请你遵守以下原则：

- 优先保证“能跑通首次登录 + session 复用 + 搜索页响应监听 + 详情页补全 + 数据导出”这条主链路
- 不要把大量篇幅浪费在花哨 UI 上
- 不要只给框架不填实现
- 不要偷懒省略关键函数
- 不要只返回一个大文件
- 不要只写思路，直接写代码
- 如果某个响应 JSON 结构可能不稳定，请把解析写得鲁棒一些
- 如果你需要假设某个字段路径，请在代码注释里明确标注“需按真实响应微调”
- 先交付最小可行版本，再补充可选增强
- 搜索结果抓取和详情页补全要分层设计，便于后续扩展评论抓取
- 评论区抓取不是本次必须主链路，除非实现成本较低且不影响主流程稳定性，否则先不要把大量精力放在评论区

十、最终目标

我的目标是：
拿到你输出的内容后，保存为一个本地项目，安装依赖后，可以完成：
- 首次人工登录
- 自动保存 session
- 自动访问小红书搜索页
- 自动监听搜索结果响应
- 自动提取笔记基础数据
- 自动进入详情页或监听详情接口补全文本与互动数据
- 自动导出表格

请直接开始输出完整项目，不要先问我确认，不要只讲思路。

补充增强要求（评论区 + 稳定性微调）：

一、评论区能力边界
1. 评论抓取作为可选增强模块实现，默认关闭，不得影响主链路（搜索结果获取 + 详情页补全）的稳定性。
2. 默认仅抓取公开可见的一级评论；二级回复作为可选项，默认关闭。
3. 评论抓取流程应为：
   搜索结果页 -> 笔记列表 -> 详情补全 -> 评论抓取 -> 评论解析 -> 评论导出
4. 增加参数：
   --with-comments true/false
   --max-comments-per-note 50
   --include-replies true/false
5. 评论抓取失败不能导致整次笔记采集失败；单条评论或单条笔记评论失败时，只记录 warning 并继续。
6. 笔记表和评论表必须分开导出，不要混成一张表。

二、评论字段（可选）
- note_id
- note_url
- comment_id
- parent_comment_id
- comment_level
- comment_content
- comment_time
- comment_like_count
- commenter_name
- commenter_id
- commenter_profile_url
- commenter_ip_location
- reply_to_user
- source_page
- source_keyword
- raw_comment_json

三、稳定性增强要求
1. 程序启动后，在真正执行搜索前，必须先做一次轻量级 session 有效性校验；校验失败再进入人工登录流程。
2. 目标响应识别必须采用“双重判定”：
   - 先按 URL 特征初筛
   - 再按 JSON 关键结构复核
   禁止只依赖单一固定 URL 字符串硬匹配。
3. 异常处理必须分层：
   - 页级失败：允许有限次重试
   - 单条笔记详情失败：记录 warning 并继续
   - 单字段解析失败：置空，不得中断整条记录
   - 评论抓取失败：不得影响笔记主流程
4. 程序必须支持渐进式落盘：
   - 每完成一页搜索结果即保存一次笔记中间结果
   - 若开启评论抓取，每完成一批评论也保存一次
   - 可选支持 checkpoint.json 断点续跑
5. 请求间隔应支持分层控制：
   - 搜索页访问间隔
   - 详情页补全间隔
   - 评论抓取间隔
6. 除 notes.* 和 comments.* 外，还应额外输出 failed_records.* 或 crawl_log.*，
   至少包含：
   - keyword
   - stage
   - target_id
   - page_no
   - error_type
   - error_message
   - occurred_at
7. raw json 默认关闭；若开启，应优先保存为单独 jsonl/json 文件，不要把大体积原始 JSON 全量塞进 Excel。
8. 评论抓取仅限公开可见评论内容；默认不做无限翻页，不尝试获取非公开或需额外权限的数据。